# flyrank-frontend

FlyRank internship — Frontend AI Engineering track capstone project. Setup in progress.

## Prerequisites

- [Node.js](https://nodejs.org/) LTS
- npm (bundled with Node.js)

The application stack will be finalized in a later assignment.
